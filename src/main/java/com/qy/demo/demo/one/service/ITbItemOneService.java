package com.qy.demo.demo.one.service;

import com.qy.demo.demo.one.entity.TbItemOne;
import com.baomidou.mybatisplus.extension.service.IService;

import java.util.Collection;
import java.util.Map;

/**
 * <p>
 * 商品表 服务类
 * </p>
 *
 * @author jobob
 * @since 2021-07-17
 */
public interface ITbItemOneService extends IService<TbItemOne> {
/*
     boolean saveBatch1(Collection<TbItemOne> entityList);
     // 插入（批量）
     boolean saveBatch1(Collection<TbItemOne> entityList, int batchSize);*/

   /*  int del(TbItemOne user);

     int del(Integer id);

     int deleteByMap(Map<String, Object> columnMap);

     int insert1(TbItemOne user);



     boolean saveBatch1(Collection<TbItemOne> entityList);
     // 插入（批量）
     boolean saveBatch1(Collection<TbItemOne> entityList, int batchSize);


     boolean remove(TbItemOne user);
     // 根据 ID 删除
     boolean removeById(Integer id);
     // 根据 columnMap 条件，删除记录
     boolean removeByMap1(Map<String, Object> columnMap);*/


    /* // 根据 UpdateWrapper 条件，更新记录 需要设置sqlset
     boolean update(Wrapper<T> updateWrapper);
     // 根据 whereWrapper 条件，更新记录
     boolean update(T updateEntity, Wrapper<T> whereWrapper);
     // 根据 ID 选择修改
     boolean updateById(T entity);
     // 根据ID 批量更新
     boolean updateBatchById(Collection<T> entityList);
     // 根据ID 批量更新
     boolean updateBatchById(Collection<T> entityList, int batchSize);

     List<T> list();
     // 查询列表
     List<T> list(Wrapper<T> queryWrapper);
     // 查询（根据ID 批量查询）
     Collection<T> listByIds(Collection<? extends Serializable> idList);
     // 查询（根据 columnMap 条件）
     Collection<T> listByMap(Map<String, Object> columnMap);
     // 查询所有列表
     List<Map<String, Object>> listMaps();
     // 查询列表
     List<Map<String, Object>> listMaps(Wrapper<T> queryWrapper);
     // 查询全部记录
     List<Object> listObjs();
     // 查询全部记录
     <V> List<V> listObjs(Function<? super Object, V> mapper);
     // 根据 Wrapper 条件，查询全部记录
     List<Object> listObjs(Wrapper<T> queryWrapper);
     // 根据 Wrapper 条件，查询全部记录
     <V> List<V> listObjs(Wrapper<T> queryWrapper, Function<? super Object, V> mapper);



     // 无条件分页查询
     IPage<T> page(IPage<T> page);
     // 条件分页查询
     IPage<T> page(IPage<T> page, Wrapper<T> queryWrapper);
     // 无条件分页查询
     IPage<Map<String, Object>> pageMaps(IPage<T> page);
     // 条件分页查询
     IPage<Map<String, Object>> pageMaps(IPage<T> page, Wrapper<T> queryWrapper);*/
}



